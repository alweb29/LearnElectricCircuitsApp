<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    tools:context=".dividers.DividersMainView"
    android:background="@drawable/background_image"
    android:layout_gravity="center">



    <TextView
        android:id="@+id/tv_current_divider"
        android:layout_width="300sp"
        android:layout_height="200sp"
        android:background="@drawable/basic_choice_tile"
        android:text="Current Divider"
        android:textSize="40sp"
        android:gravity="center"
        android:textColor="@color/white"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toTopOf="@id/tv_voltage_divider"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        />
    <TextView
        android:id="@+id/tv_voltage_divider"
        android:layout_width="300sp"
        android:layout_height="200sp"
        android:background="@drawable/basic_choice_tile"
        android:text="Voltage Divider"
        android:textSize="40sp"
        android:gravity="center"
        android:textColor="@color/white"
        app:layout_constraintTop_toBottomOf="@id/tv_current_divider"
        app:layout_constraintBottom_toTopOf="@id/ib_go_back_arrow_dividers"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        />
    <ImageButton
        android:id="@+id/ib_go_back_arrow_dividers"
        android:src="@drawable/baseline_arrow_back_24"
        android:scaleX="3"
        android:scaleY="3"
        android:background="#00000000"
        android:layout_height="100dp"
        android:layout_width="100dp"
        android:contentDescription="Go Back to main Menu"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"/>



</androidx.constraintlayout.widget.ConstraintLayout>